#!/usr/bin/python3
from setuptools import setup, find_packages

setup(
    name='fog05',
    version='0.0.1dev',
    author='ATO',
    packages=['fog05','fog05/interfaces'],
    install_requires=['networkx','']
)
